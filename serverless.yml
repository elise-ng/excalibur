service: excalibur

provider:
  name: aws
  runtime: nodejs6.10
  region: ap-northeast-1

plugins:
- serverless-webpack

custom:
  webpack:
    includeModules: true

functions:
  excalibur:
    handler: src/index.handler
    events:
      - http: ANY /
      - http: ANY {proxy+}
